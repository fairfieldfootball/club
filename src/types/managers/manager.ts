import { BaseDocument } from '@makes-apps/lib';

export interface Manager extends BaseDocument {
  email: string;
  first_name: string;
  last_name: string;
  // season performances
  // blog posts
}
