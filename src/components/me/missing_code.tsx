import React from 'react';
import { Text, Wrapping } from '@makes-apps/lib';

export default () => (
  <Wrapping limit={62}>
    <Text size="mega">
      It seems you've reached this page in error...please hit the back button or navigate elsewhere to return to the
      site.
    </Text>
  </Wrapping>
);
